mov r1, 0
mov r2, 0x20000     ; KERNEL_ADDRESS
mov r9, r2
_load_loop:

io r4, r1, r1, rom ; Load from rom
store [r2], r4

addi r1, r1, 4 ; Increment pointers, rom reads 32 bits so we inc 4 bytes
addi r2, r2, 4


addi r3, zero, 0x1000
bl r1, r3 _load_loop

jr r9