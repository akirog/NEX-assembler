mov r0, 0
mov r1, 0x1000 ; KERNEL_ADDRESS
movh r1, 0x1000 ; KERNEL_ADDRESS
mov r8, r1
_load_loop:

io r3, r0, r0, 5 ; Load from rom
store [r1], r3

add r0, r0, 4 ; Increment pointers, rom reads 32 bits so we inc 4 bytes
add r1, r1, 4

cmp r0, 0x1000
jl _load_loop

jr r8